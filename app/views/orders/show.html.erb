<div class="container">

  <div class="confirmation">
    <h1>Thank you</h1>
    <h4>You donated <%= humanized_money_with_symbol @order.amount %> to <%= @order.donation.name %></h4>
    <p>The future belongs to those who act. The future now belongs to you.</p>
  </div>

</div>

<!--

<div class="container">
  <div class="row py-4">
    <div class="col-md-3"><%= cl_image_tag @order.donation.photo, width: '100%' %></div>
    <div class="col">
      <h1>Order #<%= @order.id %> <small><span class="badge badge-primary"><%= @order.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>ISSUED ON</strong> <%= @order.created_at.to_date %></li>
        <li><strong>DONATION</strong> <%= @order.donation.name %> [<%= @order.donation_sku %>]</li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
      <p>Estimated delivery in <strong>2 business days</strong></p>
    </div>
  </div>
</div>
 -->
